// API Configuration
// In production (Render), frontend and backend are served from the same domain
// In development, Vite proxy handles /api requests
const API_BASE_URL = '';

export { API_BASE_URL };
